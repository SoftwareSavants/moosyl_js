# Releasing a New Version

1. **Bump version** in `package.json` (e.g. patch: `0.0.4` → `0.0.5`).
2. **Build**: `npm run build`
3. **Publish**: `npm publish --access public`

Ensure you are logged in to npm (`npm login`) before publishing.
